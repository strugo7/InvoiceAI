# TEMPLATE ONLY — do NOT put real values here and do NOT commit a filled copy.
#
# The real Secret is created directly from backend/.env (which is gitignored):
#
#   kubectl create secret generic invoiceai-secrets \
#     --from-env-file=backend/.env
#
# This file just documents the keys the app expects, so reviewers can see the
# shape of the Secret without exposing values.
apiVersion: v1
kind: Secret
metadata:
  name: invoiceai-secrets
  labels:
    app.kubernetes.io/name: invoiceai-backend
type: Opaque
stringData:
  GEMINI_API_KEY: "REPLACE_ME"
  SUPABASE_URL: "https://<project-ref>.supabase.co"
  SUPABASE_SERVICE_KEY: "REPLACE_ME"
  GOOGLE_CLIENT_ID: "REPLACE_ME.apps.googleusercontent.com"
  GOOGLE_CLIENT_SECRET: "REPLACE_ME"
  OAUTH_REDIRECT_URI: "http://localhost:8000/api/auth/callback"
  SESSION_SECRET: "REPLACE_ME"
  TOKEN_ENC_KEY: "REPLACE_ME"
  SHEET_ID: "REPLACE_ME"
